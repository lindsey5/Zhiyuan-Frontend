services:
  backend:
    build:
      context: .
      target: dev

    container_name: frontend_dev

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env

    command: npm run dev