services:
  frontend:
    build:
      context: .
      target: prod

    container_name: frontend_prod

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env

    command: npm start