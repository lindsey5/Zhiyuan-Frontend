services:
  frontend:
    build:
      context: .
      target: dev

    container_name: frontend_dev

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env