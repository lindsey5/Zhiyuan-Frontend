services:
  frontend:
    build:
      context: .
      target: prod

    container_name: frontend_prod

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env